{% extends 'base.html.twig' %}

{% block body %}

	<div class="jumbotron text-center">
		<h1> On progress {AGRI} TECH  * VINTAGE * NATURE >  New For you Everybody </h1> 
	</div>
<ul>
{% for category in categories %}
    <li>
    {#<a href="{{ category.name}}">{{ category.name | upper }}</a>#}
    <a href="{{ category.url }}">{{ category.name | upper }}</a></li>
    {% endfor %}
</ul>

{% endblock %}